# Authentication API

Related: [../07-security-compliance.md](../07-security-compliance.md), [../08-rbac.md](../08-rbac.md), [../errors.md](../errors.md), [../audit-events.md](../audit-events.md).

## Policy

Authentication uses local credentials initially, with future OIDC/SAML/SCIM remaining outside this milestone. Tenant-aware registration, email verification, login, logout, session management, access/refresh tokens, and password recovery are implemented incrementally through the M2 issue series.

| Method / URI                                   | Authentication                  | Authorization                        | Request                                                         | Response                                                                                      | Validation                                                                               | Errors                                                           | Page/filter/sort                               | Rate limit                     | Example                    |
| ---------------------------------------------- | ------------------------------- | ------------------------------------ | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------- | ------------------------------ | -------------------------- |
| `POST /api/v1/auth/register`                   | Public with anti-abuse controls | None                                 | Email, password, tenant hint, optional profile fields           | Accepted response without account/tenant existence disclosure                                 | Tenant active, registration enabled, email format, password policy                       | `VALIDATION_FAILED`                                              | None                                           | Strict IP/identifier throttles | Register a requester.      |
| `POST /api/v1/auth/email-verification/confirm` | Verification token              | Own account candidate                | Verification token                                              | Accepted response without token validity disclosure                                           | Token active, unexpired, purpose scoped                                                  | `AUTH_TOKEN_INVALID`                                             | None                                           | Token/IP scoped                | Verify email.              |
| `POST /api/v1/auth/login`                      | Public with anti-abuse controls | None                                 | Identifier, password, tenant hint, remember-me, device metadata | Tenant-scoped session and token pair; status is `authenticated` or `password_change_required` | Verified identifier, active user, active tenant role, password, lockout, password expiry | `AUTH_INVALID_CREDENTIALS`, `AUTH_LOCKED`                        | None                                           | Strict IP/identifier throttles | Login with email/password. |
| `POST /api/v1/auth/logout`                     | Active session                  | Own session                          | Current session ID or target own session ID                     | Accepted response                                                                             | Session belongs to actor                                                                 | `AUTH_SESSION_EXPIRED`                                           | None                                           | Actor scoped                   | Logout current browser.    |
| `POST /api/v1/auth/refresh`                    | Refresh token                   | Own token family                     | Refresh token                                                   | New JWT access token and rotated refresh token                                                | Token valid, not reused, not revoked, active session                                     | `AUTH_TOKEN_REVOKED`, `AUTH_TOKEN_REUSE_DETECTED`                | None                                           | Token-family scoped            | Rotate session token.      |
| `GET /api/v1/auth/me`                          | Active session                  | Own identity                         | None                                                            | User, memberships, selected tenant, permissions summary                                       | Active session                                                                           | `AUTH_REQUIRED`                                                  | None                                           | Normal read                    | Load current user.         |
| `POST /api/v1/auth/select-tenant`              | Active session                  | Active tenant membership             | Tenant public ID                                                | Selected tenant context                                                                       | Membership active; tenant not suspended                                                  | `TENANT_NOT_FOUND`, `AUTH_FORBIDDEN`                             | None                                           | Actor scoped                   | Switch tenant.             |
| `POST /api/v1/auth/password-reset/request`     | Public                          | None                                 | Identifier, tenant hint optional                                | Accepted response without existence disclosure                                                | Identifier format                                                                        | `VALIDATION_FAILED`                                              | None                                           | Strict anti-abuse              | Request reset email.       |
| `POST /api/v1/auth/password-reset/confirm`     | Reset token                     | Own account                          | Token, new password                                             | Accepted confirmation; existing credentials revoked                                           | Token valid; password policy                                                             | `AUTH_TOKEN_INVALID`, `VALIDATION_FAILED`                        | None                                           | Token/IP scoped                | Reset password.            |
| `POST /api/v1/auth/password/change`            | Active session                  | Own identity                         | Current password, new password                                  | `changed`; all sessions and refresh tokens revoked                                            | Session active; current password valid; new password policy                              | `AUTH_REQUIRED`, `AUTH_INVALID_CREDENTIALS`, `VALIDATION_FAILED` | None                                           | Actor/session scoped           | Change password.           |
| `GET /api/v1/auth/sessions`                    | Active session                  | Own identity                         | None                                                            | Active sessions                                                                               | Actor owns identity                                                                      | `AUTH_REQUIRED`                                                  | Cursor, filter by state/device, sort last seen | Normal read                    | Manage devices.            |
| `DELETE /api/v1/auth/sessions/{session_id}`    | Active session                  | Own session or `user.session.revoke` | Target session                                                  | Revocation result                                                                             | Session belongs to actor/tenant scope                                                    | `AUTH_FORBIDDEN`, `NOT_FOUND`                                    | None                                           | Actor scoped                   | Revoke device.             |

## Security notes

Authentication errors must not disclose account, tenant, or membership existence. Privileged actions require step-up authentication or phishing-resistant MFA once policy is finalized.

All public credential and token mutations use a 60-second fixed window by default. Default limits are login 5, registration 5, verification 10, reset request 5, reset confirmation 10, password change 5, and refresh 20. Limits are keyed by endpoint scope plus hashed tenant, identifier, token, session, device, and IP dimensions. A rejected bucket returns HTTP 429 with `Retry-After` and records `auth.rate_limit.exceeded`.

M2.06 issues signed JWT access tokens with minimal user, tenant, and session claims, and stores only hashed refresh tokens. Refresh tokens rotate on every refresh; replay of a rotated/revoked token revokes the token family and related session.

Browser authentication uses the same-origin Next.js BFF selected in [../adr/ADR-0005.md](../adr/ADR-0005.md). Browser pages call same-origin BFF routes, which store session material only in `HttpOnly`, `Secure`, `SameSite=Lax` cookies and forward authenticated API calls server-side. Refresh tokens, session identifiers, and long-lived credentials must not be stored in `localStorage`, `sessionStorage`, or any browser-readable token cache. Cookie-authenticated state-changing requests require BFF-level CSRF validation before the API request is proxied.

API-to-API clients use bearer tokens. Local session-management endpoints may still pass the current session with `x-session-id` only as temporary development transport until M2.12b/M2.12c replace protected browser flows with bearer-guarded API calls and BFF cookie handling.

M2.07 stores only SHA-256 hashes of cryptographically random password-reset tokens. Reset requests always return an accepted response, tokens are purpose-scoped, single-use, and expire after `PASSWORD_RESET_TOKEN_TTL_MINUTES` (60 by default). A successful reset applies the password policy, replaces the Argon2id hash, and revokes all active sessions and refresh tokens for that tenant/user identity.

M2.08 returns `password_change_required` after valid login when `passwordExpiresAt` has elapsed and sets `pwd_change_required=true` in both initial and rotated access tokens. The restricted session exists only to permit credential recovery until authorization guards enforce the claim across protected business routes. Authenticated password changes verify the current password, reject current-password reuse, calculate the next expiry from `tenant.passwordExpiresDays`, revoke all tenant-scoped sessions and refresh tokens, and require reauthentication.

M2.09 tracks failed passwords against the user identity using the selected tenant's lockout threshold, failure window, and lock duration. Lockout and invalid-credential responses remain identical to callers. Successful login after lock expiry clears failed-attempt state. Audit metadata contains only hashed IP and device identifiers.

M2.11 records registration, email verification, login success/failure, logout, session revocation, refresh success/failure/reuse, password reset request/rejection/completion/replay, password change, lockout, and rate-limit events. Every event carries a correlation ID and privacy-minimized request hashes when request data is available; tenant, actor, and target fields are populated when known. A shared recursive redaction boundary removes passwords, tokens, secrets, cookies, authorization values, and credentials before persistence. Auth operations await audit persistence and fail closed when an audit event cannot be written.
